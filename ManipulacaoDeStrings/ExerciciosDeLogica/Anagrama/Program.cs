// See https://aka.ms/new-console-template for more information
Console.WriteLine("Hello, World!");

// Verifique se duas palavras digitadas pelo usuário são anagramas uma da outra.
// Duas entradas 

/*
Console.WriteLine("Informe a primeira palavra: ");
string palavra1 = Console.ReadLine();

Console.WriteLine("Informe a segunda palavra: ");
string palavra2 = Console.ReadLine();

if (palavra1.Length != palavra2.Length)
{
    Console.WriteLine("As palavras não são anagramas");
}

char[] palavraSeparada1 = palavra1.ToCharArray();
char[] palavraSeparada2 = palavra2.ToCharArray();

Array.Sort(palavraSeparada1); // Pega o array e coloca em ordem alfabetica
Array.Sort(palavraSeparada2);

for (int i = 0; i < palavraSeparada1.Length; i++)
{
    if (palavraSeparada1[i] != palavraSeparada2[i])
    {
        Console.WriteLine("As palavras não são anagramas.");
        return;
    }
}

Console.WriteLine("As palavras são anagramas!");
*/

